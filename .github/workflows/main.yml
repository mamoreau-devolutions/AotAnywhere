name: CI

on:
  pull_request:
    branches: [ master ]
    # Docs/site-only PRs don't affect the build, so skip this CI for them.
    # Keep in sync with the paths-ignore in cross-platform-validation.yml.
    paths-ignore:
      - 'site/**'
      - 'docs/**'
      - '**/*.md'
      - 'LICENSE.TXT'
      - '.github/workflows/deploy-site.yml'

# Releases are driven by publish.yml (dispatch it with a version); this is PR CI
# only. A new push to the same PR supersedes the running build.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_and_test:
    strategy:
      matrix:
        include:
          - vm: ubuntu-latest
    runs-on: ${{ matrix.vm }}
    name: Build and test ${{ matrix.vm }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
      - name: Setup dotnet
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '8.0.x'
      # The Linux smoke build uses the runner's Clang and host sysroot. Package
      # consumption with restored content packages is covered separately.
      - name: Cache NuGet packages
        uses: actions/cache@v6
        with:
          path: ~/.nuget/packages
          key: nuget-ci-${{ runner.os }}-${{ hashFiles('src/AotAnywhere.nuproj', 'src/Crosscompile.targets', 'test/Hello.csproj') }}
          restore-keys: |
            nuget-ci-${{ runner.os }}-
      - name: Install Clang, LLD, and UPX
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y clang lld
          curl -sSfL -o "$RUNNER_TEMP/upx.tar.xz" https://github.com/upx/upx/releases/download/v5.0.2/upx-5.0.2-amd64_linux.tar.xz
          tar -xJf "$RUNNER_TEMP/upx.tar.xz" -C "$RUNNER_TEMP"
          echo "$RUNNER_TEMP/upx-5.0.2-amd64_linux" >> "$GITHUB_PATH"
      - name: Build
        shell: bash
        run: |
          dotnet build -t:Pack src/AotAnywhere.nuproj
          # Default publish: StripSymbols=true for Linux targets, served by
          # the managed AotAnywhereStrip task (no LLVM on the runner).
          dotnet publish -r linux-x64 -c Release \
            -p:UseExternalClang=true -p:AotAnywhereLinuxSysroot=/ \
            test/Hello.csproj
          # The strip pipeline must have produced the symbol sidecar.
          test -f test/bin/Release/net8.0/linux-x64/publish/Hello.dbg
          # Opt-in UPX compression. Same obj, so the link is up to date and
          # only the copy + pack steps run; publish to a separate folder so
          # both variants reach the validate job. UPX packs the published copy
          # in this folder, leaving the shared obj (and the default publish
          # above) uncompressed.
          dotnet publish -r linux-x64 -c Release \
            -p:UseExternalClang=true -p:AotAnywhereLinuxSysroot=/ \
            -p:AotAnywhereUpxCompress=true -o test/bin/upx-publish test/Hello.csproj
      - name: Upload test binary
        uses: actions/upload-artifact@v7
        with:
          name: Hello
          path: test/bin/Release/net8.0/linux-x64/publish/Hello
      - name: Upload UPX-compressed test binary
        uses: actions/upload-artifact@v7
        with:
          name: HelloUpx
          path: test/bin/upx-publish/Hello

  validate:
    needs: build_and_test
    runs-on: ubuntu-latest
    name: Try launching
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
      - name: Launch
        run: |
          # download-artifact places each artifact at a path that varies with
          # the action version, so locate the binaries by their artifact
          # directory rather than hardcoding ./Hello/Hello.
          bin=$(find . -type f -path '*/Hello/Hello' | head -1)
          upxbin=$(find . -type f -path '*/HelloUpx/Hello' | head -1)
          if [ -z "$bin" ]; then echo "Hello binary not found" >&2; exit 1; fi
          if [ -z "$upxbin" ]; then echo "HelloUpx binary not found" >&2; exit 1; fi
          # The binary was stripped by the managed AotAnywhereStrip task;
          # check binutils can parse the rewritten section table and that
          # the strip actually happened, then run it.
          readelf -S "$bin" > sections.txt
          grep -q '\.gnu_debuglink' sections.txt
          if grep -qE '\.symtab|\.debug_info' sections.txt; then echo "strip failed: symbol/debug sections still present" >&2; exit 1; fi
          echo "Launching $bin"
          chmod +x "$bin"
          "$bin"
          # The UPX variant must actually be packed (magic present, smaller
          # than the plain binary) and still run - UPX unpacks it in memory
          # at launch.
          grep -q 'UPX!' "$upxbin" || { echo "UPX magic not found in compressed binary" >&2; exit 1; }
          plain_size=$(stat -c%s "$bin"); upx_size=$(stat -c%s "$upxbin")
          echo "Plain: $plain_size bytes, UPX: $upx_size bytes"
          if [ "$upx_size" -ge "$plain_size" ]; then echo "UPX-compressed binary is not smaller than the plain one" >&2; exit 1; fi
          echo "Launching $upxbin"
          chmod +x "$upxbin"
          "$upxbin"
